//! Id-expression related display
//!
//! In the C++ grammar, an id-expressions is a generalization of the notion of
//! identifier that includes qualified identifiers (`path::to::something`) and
//! templated names (`A<B>`).

mod atoms;
// mod scopes;
// mod unqualified;
